package com.czm.d10_interface_attention;

public class Test {
    /*
     1、接口其他注意事项(了解)
        1、一个接口继承多个接口，如果多个接口中存在方去签名冲突，则此时不支持多继承。

        2、一个类实现多个接口，如果多个接口中存在方法名冲突，则此时不支持多实现。

        3、一个类继承了父类，又同时实现了接口，父类中接口中有同名的默认方法，实现类会优先用父类的。

        4、一个类实现了多个接口，多个接口中存在同名的状认方法，可以不冲突，这个类重写该方法即可。

     */
    public static void main(String[] args) {

    }
}
