<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.czm.mapper.UserMapper">
    <!-- 查询全部数据 -->
    <select id="findList" resultType="com.czm.entity.User">
        select * from user
    </select>
</mapper>